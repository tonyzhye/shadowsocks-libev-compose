# Shadowsocks-libev Docker Compose
Use [docker-compose] to manage Shadowsocks-libev docker containers.

## Edit .env for secret settings

## Run docker compose

```bash
$ docker-compose up -d
$ docker-compose ps
```
## Finish
